{
  "name": "gravity-push",
  "version": "1.0.0",
  "description": "A custom action to send a Web Push notification to a Gravity Topic via the Gravity REST API, authenticated by an API key",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "snakeNet",
    "Gravity",
    "Web Push",
    "API key",
    "CI/CD"
  ],
  "author": "Marc Fiedler <dev@snakenet.org>",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@actions/core": "^3.0.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.45.0"
  }
}
