{
  "name": "pulsar-push",
  "version": "1.3.0",
  "description": "A custom action to send a Web Push notification to a Pulsar Topic via the Pulsar REST API, authenticated by an Access Key",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "snakeNet",
    "Pulsar",
    "Web Push",
    "Access Key",
    "CI/CD"
  ],
  "author": "Marc Fiedler <dev@snakenet.org>",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@actions/core": "^3.0.1"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.45.0"
  }
}
